//setTimeout(callback, interval loop)
//takes
setTimeout(() => {}, timeout);

// 1) Select a node
//querySelector is a css selector
let node = document.querySelector("#someID");

// addEventListener('eventType', callback)
node.addEventListener();

// add a child to the document object
document.createElement("div");

//adds a child to the selected node
let ch = document.createElement("button");
let body = document.querySelector("body");
body.appendChild(ch);
